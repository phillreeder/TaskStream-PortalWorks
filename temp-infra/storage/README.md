# Temporary POC storage

Runtime SQLite databases are created beneath this directory and are disposable.
The current POC default is `IEBBeta/Test1/task-storage.sqlite`.

This is runtime infrastructure only. It is not the final TaskStream storage model.
